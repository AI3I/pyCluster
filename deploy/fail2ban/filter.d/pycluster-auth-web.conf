[Definition]
# Matches structured pyCluster authentication failures written to
# /var/log/pycluster/authfail.log for the public web login path.
failregex = ^.*AUTHFAIL channel=public-web ip=<HOST> call=[^ ]+ reason=[a-z_]+$

ignoreregex =
