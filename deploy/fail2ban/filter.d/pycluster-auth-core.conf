[Definition]
# Matches structured pyCluster authentication failures written to
# /var/log/pycluster/authfail.log for core-service paths.
failregex = ^.*AUTHFAIL channel=(?:telnet|sysop-web) ip=<HOST> call=[^ ]+ reason=[a-z_]+$

ignoreregex =

