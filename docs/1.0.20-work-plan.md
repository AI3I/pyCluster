# 1.0.20 Work Plan

This is the implementation checklist agreed on September 5, 2026. It records
progress separately from the unscheduled future-feature concepts.

## Dashboard Diagnostics

- [x] Simplify Peers & Links and separate PC/PY evidence in Protocol Health.
- [x] Add PC/PY history selection and filter on the server before limiting rows.
- [x] Preserve traffic counters in Protocol Health.
- [x] Run the focused SysOp regression suite (95 tests passed).
- [x] Verify Protocol Health and Security layouts in Firefox at 1400, 768,
  and 390 pixels; exercise history switching and block actions with synthetic data.
- [x] Deploy and validate service health on pycluster.ai3i.net.

## Local Address Blocking (#277)

- [x] Define persistent IPv4/IPv6 address and CIDR blocks with reason, author,
  creation time, expiration, and removal history.
- [x] Resolve client addresses through explicitly trusted proxies, walking
  forwarded chains from the socket towards the first untrusted hop.
- [x] Enforce before telnet login/account creation and web authentication.
- [x] Provide SysOp and telnet management using the same backend.
- [x] Test IPv6, mapped IPv4, CIDR boundaries, expiration, proxy spoofing,
  privilege checks, and persistence across database reopening.
- [x] Verify block controls with synthetic browser responses and validate
  persistence across service restarts on pycluster.ai3i.net.

Local validation on September 5: the configuration, address-policy, public-web,
SysOp, and security suites passed 192 tests (two deselected by the test
configuration). After correcting mobile grid overflow and separating PC/PY
counters, the affected suites passed 100 tests. Firefox automation reported no
JavaScript errors or page overflow. Address-block and history controls were
exercised against synthetic responses; this does not establish live deployment
or restart behavior. SSH to pycluster.ai3i.net succeeded and both services were
active; no runtime files, accounts, or policies were changed during validation.

Deployment validation subsequently completed on September 5. The standard
upgrade script backed up runtime configuration, data, and logs to
`/root/pycluster-backups/upgrade-preflight_20260905_235410.tar.gz`. Both runtime
configuration checksums remained unchanged. Three telnet listeners and both
web services passed readiness checks. Temporary documentation-address blocks
persisted across a service restart; both running HTTP services returned 403
with matching trusted-proxy headers, then 200 after removal. The deployed
telnet handler was separately exercised with synthetic IPv4, IPv6, and mapped
IPv4 writers against the runtime database. No test block remains active; the
two removed audit records identify AI3I-99. No spots or announcements were
posted and no test user accounts were created.

## Effective-State Diagnostics And Filters

- [ ] Share evaluation of access, subscriptions, filters, and throttling.
- [x] Explain stored-rule acceptance/rejection of a sample spot without transmitting it.
- [x] Add a two-condition AND editor and equivalent telnet commands.
- [x] Preview drafts before saving; validate length and incomplete recognized conditions.
- [x] Require confirmation before quick filters replace existing slot-8 rules.
- [x] Verify ordinary-spot and RBN rule previews across web and telnet.

The shared rule decision now drives live evaluation and telnet previews. Web
previews additionally check web RBN access/subscription. Feed availability,
ingest policy, browser filters, and live throttling remain outside preview
scope. Full graphical expression trees, state selection, exhaustive syntax
validation, and revision-based concurrent-edit protection remain future work.

Validation: the public-web, telnet-command, and shared-filter suites passed
273 tests (24 deselected). A subsequent shared-filter, address-policy, and
SysOp-web run passed 111 tests, including added expression-validation cases.
These runs overlap and are not a full regression run. A local Firefox fixture
at desktop and mobile widths exercised the compound editor and preview handler
without JavaScript errors; its mocked response does not validate a live API.
The API test independently verifies draft previews do not save rules or spots
and preserves spotter SSIDs across web and telnet matching. Deployment results
for the filter changes are recorded below.

Follow-up regression: 689 default tests passed locally, and all 49 explicitly
marked listener tests passed in an isolated Debian checkout on the staging host.
The listener tests use temporary databases and loopback ports, not production
accounts or cluster links. The new cross-surface test checks telnet-created rules,
web deletion, global rejects overriding RBN accepts, and subscription/access gates.
It confirms previews store no spots. The initial new fixture omitted an active
telnet session; that fixture was corrected before the clean full-suite run.

## Interoperability And Release

- [x] Run existing compatibility regressions for prompts, password echo, CC11,
  reconnects, and PC/PY negotiation; all listener tests passed.
- [x] Update strings, command documentation, and operational documentation.
- [x] Run the complete default and explicit listener regression suites.
- [x] Deploy the tested filter changes and verify service health.
- [ ] Present results for operator acceptance before tagging/releasing.

Application passwords, PY14 advisories (#276), and the topology graph are
scheduled for a subsequent planning cycle. No release or deployment is implied
by a checked implementation item above.

### Committed Deployment Validation

Commit `e691aac` was deployed to pycluster.ai3i.net on 2026-09-06 UTC from a
clean checkout at `/usr/src/pyCluster-1.0.20-validation`. The previous modified
source checkout was retained. The upgrade backed up runtime config, data, and
logs to `/root/pycluster-backups/upgrade-preflight_20260906_004236.tar.gz`.
Both node configuration files retained their pre-upgrade SHA-256 checksums.
Both services, all three telnet login prompts, and public HTTPS health passed;
the service journals contained no error-priority entries after the upgrade.

For #277, temporary documentation-only IPv4/IPv6 blocks produced HTTP 403 on
both running web services, followed by HTTP 200 after removal. Synthetic telnet
writers confirmed IPv4, IPv6, and mapped-IPv4 rejection before login. Those are
handler tests, not connections originating from real external IPv6 addresses.
All temporary blocks were removed. No test accounts were created or spots or
announcements posted. The graphical filter workflow still awaits operator
acceptance on the deployed UI; no tag or release was created.
